# dad — changelog

## 2026-07-21 — I64 skipped Schedule row badges (local-only, unpublished)
- Added read-only `SKIPPED` / `SKIPPED ×N` badges inside the selected Schedule's ordered Preset cells. The internal
  projection uses only the active non-dry run or latest non-dry run, available exact retry lineage, exact
  Schedule/run/entry IDs, and terminal scheduler `Skipped` results; repeated iterations aggregate and duplicate
  iterations are suppressed while existing result summaries provide the hover detail.
- When bounded scheduler history no longer retains every row detail, the existing run aggregate shows a compact total
  versus retained-detail notice without inventing badges. The orange would-be-skipped LevelSeek preview, editing,
  locks, ordering, repeats, execution, persistence, configuration, IPC/retries/public types, versions, website,
  Aethertek page, W19/P1204, I63, AutoParty commit, packaging, and publication remain unchanged.

## 2026-07-20 — v0.4.1.3 in-plugin Discord pairing and measured DAD pilot (local-only, unpublished)
- Migrated AutoParty configuration from schema 5 to 6, disabled by default, with CurrentUser-DPAPI bot-token references,
  authenticated Discord Application/Bot User IDs, channel binding, coordinator-star pairings, and persistent measured-pilot evidence.
- Added pinned `Discord.Net.WebSocket` 3.20.1 inside every DAD instance. Signed bounded `dad.pairing/v1` presence and pairing
  envelopes reject wrong authors/channels, stale messages, replays, identity/fingerprint drift, and Application-ID reuse.
  Reconnect uses bounded backoff and one editable presence message; Discord has no plan, schedule, Stop, or execution route.
- Retired the file courier from active runtime while retaining its historical source/config fields. Raised the DAD LAN hub
  protocol to 3 so presence includes public Discord Application ID, endpoint fingerprint, and pairing health.
- Added Coordinator controls for Start measured pilot, Stop & Evaluate, and Resume. Typed run, requested-job, Stop-all,
  cleanup, LAN-participant, Discord-health, revoke, and re-pair evidence produces an atomic Ed25519-signed
  `dad.pilot-evidence/v1` receipt bound to the Coordinator identity and exact assembly SHA-256. Ordinary LAN profile
  restoration now reports `not-applicable` unless a real profile was captured and restored.
- Bumped the local pilot package to `0.4.1.3` and included Discord.Net runtime dependencies plus a separately named
  .NET Framework BouncyCastle verifier used only by the Windows PowerShell 5.1 receipt evaluator. No tag, release,
  publication, or push is part of this change.

## 2026-07-20 — I60 seven guarded party-teardown attempts (local-only, unpublished)
- Raised the bounded successful-run teardown window from three attempts at ten-second intervals to seven attempts at
  eight-second intervals, retaining the existing 60-second timeout and all party, leader, duty, queue, and prompt guards.
- Replaced the one-time approval latch with the approved command-attempt number, so an approved but ineffective breakup
  can retry only after its throttle and a later attempt can approve only its own newly appeared confirmation. Each
  cross-world command attempt remains limited to one PartyMemberList leave callback, and no eighth command is sent.
- Added focused regressions for the 0/8/16/24/32/40/48 cadence, fresh callback/prompt handling after an ineffective
  approval, sustained solo completion, guarded retry blocking, and timeout reporting. Services, coordinator,
  configuration, IPC/DTOs, manifests, README, website, version `0.4.1.1`, and publication state are unchanged.

## 2026-07-19 — P1204 mixed VERMAXION reservation-client recovery (local-only, unpublished)
- Preserved the coordinator/scheduler wake `OperationToken` while assigning each actual VERMAXION reservation epoch a
  fresh internal opaque token. Reservation authority snapshots, the initial reserve, automatic renewal, exact-token
  cleanup, and grant-event matching now share only the active epoch token.
- Retains an epoch token through failed cleanup and allocates the next token only after verified release. Stale grants
  from prior attempts are ignored, so a pre-v0.3.0.1 provider's terminal same-token `Released` behavior cannot trap a
  logical wake that retries against a mixed client fleet.
- Added deterministic regressions for the legacy `Released` trap, stable logical identity, renewal/retry token
  lifecycle, failed cleanup, stale/current grants, zero pre-grant mutation, and existing exactly-once reset/relog
  boundaries. VERMAXION, public IPC/DTO/configuration/manifest shapes, version `0.4.1.0`, release artifacts, client
  folders, AR fallback, home-world return, and publication state are unchanged.

## 2026-07-19 — P1203 shared pilot exchange root (local-only, unpublished)
- Added persisted **Pilot exchange root** support to `/dad autoparty`, defaulting new and legacy schema-5 configurations to `Z:\autopartypilot`. Apply is allowed only while transport, pairing, and execution are disabled; it validates and probes a fully qualified non-root drive or UNC path, creates `pilot-input`, `pilot-receipts`, `pilot-courier`, and `plugin`, and reports safe failure codes.
- Identity/registration exports and `pilot-fixture.json` now use `pilot-input`, status receipts use `pilot-receipts`, and the courier dynamically resolves `pilot-courier` for every operation. A successful root change therefore applies immediately without reloading DAD.
- Fresh locked x64 Debug/Release builds pass with zero warnings/errors, focused formatter verification is clean, 33/33 focused tests pass, and the complete suite passes 1,070/1,070. Version `0.4.1.0`, configuration schema 5, IPC v1, LAN behavior, Fleet TSV, and release artifacts are unchanged; this work remains one local commit and is not pushed, tagged, packaged as a release, or published.

## 2026-07-19 — I51–I53 successor batch wizard and acknowledged title login (local-only, unpublished)
- Added `/dad batch` and a Plans-page entry for a standalone session-draft Batch Preset Wizard. Explicit rotating
  accounts/characters are zipped by stable pool-local order, exact anchors are mapped per named non-overlapping DC
  pool, and selected ordinary Plan templates generate deterministic append-only Plans, per-template Schedules, and an
  optional pool/crew/template-interleaved combined Schedule. Source templates remain unchanged; output is bounded at
  512 Plans/entries and collisions, shortages, invalid lanes, substitutes, and AutoParty proposal templates block.
- Preview reports unused rotating rows and visible non-blocking out-of-pool anchor reuse. Apply requires unchanged
  Plan/Schedule source state and uses one in-memory transaction; the one session-only Undo requires the exact unchanged
  post-apply fingerprint and refuses drift. No wizard draft, management IDs, or undo snapshot enters configuration.
- A DailyReset template can set the existing per-primary **Daily** flag for all generated rows; leaving that option
  off preserves the source template flags. The roulette reward probe, checked-row routing, unanimous-received skip
  rule, and fail-open ordinary execution semantics are unchanged.
- The isolated AutoRetainer-owned `_TitleMenu` path now requires readable idle Lifestream and fresh `CanAutoLogin`
  proof before its one-time `/ays m d`. After Multi Mode is proven off it uses acknowledged
  `Lifestream.ConnectAndLogin(name, homeWorld)`: accepted proceeds without replay, explicit false retries only after a
  five-second cadence and fresh proof, and IPC uncertainty blocks without replay. Existing in-world `/ays relog`,
  home-world return, reservation, reset, cleanup, public IPC/share/configuration schema, version `0.4.0.1`, packaging,
  release artifacts, AutoParty/Fleet, Leveling Mode, and publication state are unchanged.
- Fresh Debug x64 verification passed 188/188 focused compatibility tests and 1,063/1,063 complete tests; the isolated
  plugin build completed with zero warnings and zero errors.

## 2026-07-18 — P1203 AutoParty pilot runtime (local-only, unpublished)
- Added `/dad autoparty` with explicit endpoint generation, public identity export, enrollment receipt import, independent transport/pairing/execution controls, local pairing confirmation, pilot status export, key rotation/revocation, and Owner Stop. All gates remain disabled by default.
- Added CurrentUser-DPAPI Ed25519/X25519 endpoint private-key custody. Public `.apidentity`, `.apregistration`, and `.apreceipt` packages are bounded, validate fingerprints and the exact pilot artifact SHA-256, and contain no player names, CIDs, account IDs, webhook credentials, or private keys.
- Attached a real bounded outbound-only file courier beneath the isolated pilot runtime. It uses atomic, exact-recipient envelope files, acknowledgement cleanup, hostile-file quarantine, and bounded spools without changing LAN transport or opening an inbound listener.
- Wired typed AutoParty execution through the existing DAD coordinator projection. AutoParty proposal IDs and formation-only state remain local configuration fields excluded from public IPC JSON; complete and single-client formation paths hold at `GroupReady` until local Stop/disable/expiry/revocation.
- Added the fixed-root formation-only pilot fixture importer. It requires the verified pilot artifact SHA-256, completed local pairing, exact enrolled public fingerprints, one consented queue authority, a bounded numeric requested job for every island, and a safe Content Finder condition before adding its isolated planner group and separate remote bindings.
- Kept Fleet TSV at exactly nine columns and stored consented remote bindings separately. Existing LAN behavior, IPC v1, configuration schema 5, public/plugin/file/assembly version `0.4.0.1`, and release artifacts remain unchanged. These source changes are intentionally unstaged, uncommitted, and unpublished.
- Fresh locked x64 Debug/Release plugin and test builds complete with zero warnings, and the unfiltered suite passes 1,047/1,047. No DAD file is staged or published.

## 2026-07-18 — P1184 transactional Fleet/Crew Matrix
- Added a separately disabled local Fleet/Crew Matrix window at `/dad fleet`: a bounded 160-row inventory, 40 ordered
  Crew Sets of up to eight members, and reusable duty blueprints that deterministically generate canonical DAD Plans and
  manual or daily-reset Schedules. The 160-row/40-party fixture is covered without introducing a second execution model.
- Added exact-schema bounded TSV import/export with control-character, duplicate, partial-assignment, size, and spreadsheet
  formula-prefix defenses. Import changes only the local Matrix draft; non-mutating preview reports stable generated IDs,
  local queue authority, Plan/Schedule counts, and blocking validation before any apply.
- Matrix apply is off by default, rejects active DAD/Scheduler/Schedule work and unowned deterministic-ID collisions, and
  replaces only previously Matrix-managed assets as one configuration revision. A durable one-level snapshot restores the
  exact prior Plan and Schedule collections; rejected persistence rolls the entire in-memory transaction back.
- AutoParty transport, pairing, execution, endpoint identity, and network initialization remain disabled. Existing LAN,
  public IPC v1, direct Plan/Schedule behavior, public version `0.4.0.1`, and publication state remain unchanged.
- Fresh-cache locked restore, x64 Debug/Release warnings-as-errors builds, targeted formatting, and the complete unfiltered
  1,040/1,040-test suite pass with zero skipped tests.

## 2026-07-18 — P1184 disabled AutoParty integration boundary
- Advanced only persistent configuration to schema v5. Every pre-v5 migration receives a new disabled AutoParty object
  with no identity, registration, pairing, grant, listing, secret access, connector attachment, or network initialization;
  DAD's public plugin/file/assembly version remains `0.4.0.1`.
- Added the portable `Dad.AutoParty.Protocol` `0.1.0-preview.2` package boundary, a separate disabled Discord courier
  connector, CurrentUser-DPAPI endpoint identity references, explicit registration replacement confirmation, review-gated
  fingerprint/code pairing, immutable listings/grants, unpair/privacy purge, and independent transport/policy/execution
  status. Existing LAN `DadTransportService` and public DAD IPC are unchanged.
- Added local policy gates for identity, replay, exact grant intersection, one active session per island, reservation,
  preflight, lease, strict requested-job lending, revocation, and owner veto. Stop, DAD disable, local safety, expiry, and
  revocation take precedence over remote operations.
- Added typed Prepare, Reserve, Form, Queue, Cancel, Settle, and Restore boundaries backed only by a fake execution facade.
  Ephemeral invite locators, observed-party receipts, and integration-profile capture/apply/verify/restore are bounded and
  redacted. The appended `GroupReady` phase ends formation-only work while denying queue and settle behavior.
- Added a scheduler-only authorization wait for requests carrying an explicit AutoParty proposal ID. Empty IDs preserve
  existing direct preset, Schedule, local, LAN, VERMAXION, and IPC behavior. AutoParty remains disabled and unpublished;
  no live Discord operation, production identity, service installation, or FFXIV execution is enabled by this change.

## 2026-07-18 — P1184 prerequisite P1178 selected-duty proof repair
- Restored the regular Duty Finder join requirement that `AgentContentsFinder.InterfaceSub.SelectedDutyId` exactly match
  the requested Content Finder condition alongside the stable mapped row, character, callback ordinal, and selected
  agent type/id. A contradictory interface selection can no longer authorize Join.
- Added a bounded API15-lag gate after the exact row callback: DAD waits at most six seconds and requires two exact
  interface-selected-duty observations at least 250 milliseconds apart. Timeout, drift, or any mapping/agent conflict
  restarts the full clear/hydrate/select safety cycle instead of weakening the proof.
- The shared regular-duty executor covers direct preset and Schedule starts. Public IPC, LAN transport, configuration,
  manifests, plugin version `0.4.0.1`, and release artifacts are unchanged.

## 2026-07-17 — P1199 planner persistence and Leveling activation
- Replaced direct shared-configuration writes with a thread-safe end-of-framework persistence coordinator. Same-frame
  changes coalesce after 250 quiet milliseconds with a one-second maximum delay; ordinary failures receive three
  five-second retries, native invalid-handle failures latch immediately, and no storage exception reaches window Draw.
  The main window reports memory-only changes with an explicit end-of-frame retry, successful writes clear the fault,
  and shutdown forces one final write after pending UI and roster changes.
- Compact durable history snapshots now retain the 50-entry wire shape, visible status/timing/task data, stop progress,
  step results, warnings, and scheduler failure evidence while removing full requests, participants, leases, executor
  state, client/session identifiers, and authority endpoints. Startup compacts legacy history idempotently and saves
  only when it changed; current runs and issue-report inputs remain full fidelity.
- Leveling Mode activation now validates the visible planner draft. Supported Leveling/NPC Duty Support or Trust and
  Duty Finder/Premade selections atomically update the selected preset through the established planner-field rules,
  preserve crew and Leveling settings, and enable immediately. Unsupported drafts remain disabled with exact guidance;
  Run Family and Submode are locked while enabled, and disabling changes only the enabled flag.
- Public IPC, sharing, manifests, configuration schema, plugin version, and release artifacts are unchanged.

## 2026-07-17 — I41–I43 Leveling Mode job, duty, and loop controller
- Added a default-off saved-preset Leveling Mode for Duty Support, Trust, and Premade Duty lanes with one plan goal,
  deterministic lowest-first or highest-below-goal job order, and an ordered minimum-level/duty threshold table.
  Plan/share schema v2 preserves the nested settings and legacy shares/configurations remain disabled by default.
- Added strict compilation from fixed account/character rows and complete XADB ledgers. Eligible jobs are unlocked full
  combat jobs matching each row's role (`Any` spans roles); base classes, limited jobs, missing jobs, partial/unknown
  ledgers, invalid/out-of-order thresholds, unavailable or lane-incompatible duties, party-size mismatches, and missing
  applicable thresholds block without choosing a fallback. Completed rows retain a compatible current job or use their
  highest compatible job as fillers, and the plan completes only when every active row is complete.
- Added a runtime-only outer controller that freezes a unique ordinary one-run child with the selected jobs, synced duty,
  and existing lane executor. Successful children refresh exact XADB truth before recompilation; child failure, timeout,
  route loss, or cancellation terminates without replay. Existing child reconnect behavior is retained, parties are not
  retained between children, and no Leveling-specific reload persistence, resume cursor, runner, IPC, or deployment path
  was added.
- Added planner controls and live compiler evidence. While enabled, fixed job, fixed duty, LevelSeek, and ordinary stop
  controls are visibly disabled without deleting their saved values. Plugin/manifest versions and release artifacts are
  unchanged.

## 2026-07-17 — I47 Daily-reset roulette reward preflight
- Added a default-off **Daily** checkbox per Daily Roulette planner row and preserved it through normalization,
  substitutes, effective-slot projection, clone/save, and schema-v2 Plan/Schedule sharing; schema-v1 shares import it
  unchecked. It activates only for a Daily Roulette preset materialized by a `DailyReset` Schedule.
- LevelSeek remains the first skip decision. The reward preflight wakes only checked effective rows, one at a time,
  and uses an internal typed request with exact schedule, run, entry, slot, worker route, account, character, Content ID,
  roulette ID/key, operation ID, and timestamp echoes. Stale, contradictory, disconnected, timed-out, unknown, and
  not-received outcomes all fail open to ordinary preset execution.
- A worker navigates Duty Finder only when it proved the window was closed, reads a pre-existing window only when it
  already shows the exact roulette, requires two stable exact identity/reward observations, and closes only a window
  DAD opened. The Schedule entry skips only when every checked row returns received, records a distinct reward skip
  kind/summary through the existing skipped-entry counter, and reuses existing wake cancellation cleanup.
- Existing public IPC, protocol version, ordinary worker commands, queue executors, non-DailyReset execution,
  manifests, plugin version, and release artifacts are unchanged.

## 2026-07-17 — I38 AutoRetainer-owned title-idle login
- Added an isolated logged-out wake branch that requires the exact connected stable-account route and configured
  catalog character, fresh ready `_TitleMenu` evidence with no navigation/connection/error overlay, readable idle
  AutoRetainer IPC, Multi Mode ownership, and no conflicting suppression or automation hold.
- The branch sends `/ays m d` once, verifies Multi Mode is disabled, then sends the existing exact-character relog
  once and waits without replay for world readiness. Generic/manual title screens, unknown or contradictory state,
  route loss, and cancellation issue no later command; DAD never launches a closed process.
- The existing in-world VERMAXION/AutoRetainer reservation, reset, relog, home-world, cleanup, IPC, configuration,
  manifest, and version behavior is unchanged.

## 2026-07-17 — I39 LevelSeek Schedule indication
- Schedule preset rows now turn orange only when the scheduler's current substitute-bound LevelSeek evaluation proves
  every targeted row is satisfied. Hovering the preset shows the evaluator summary and exact per-slot evidence.
- Below-target, unknown, absent, untargeted, and missing-preset rows retain normal coloring. The indication is transient;
  scheduler execution, skip history, persisted Schedule data, public IPC, manifests, and versions are unchanged.

## 2026-07-17 — P1197 worker acknowledgement race and Schedule resume
- Remote execution now waits for the worker's real exact-command acknowledgement before status polling. Older-run or
  older-command replies are discarded and polled again; current-command role, module, worker, and identity conflicts
  still fail closed through the existing strict validator.
- Added the operator-only **Resume from failed entry** path for coordinator/plugin reload abandonment. It creates a new
  run at the exact persisted cursor, retains history, excludes active work, never replays automatically, and leaves
  Cancel terminal and non-resumable.
- Coalesced unchanged successful XA Database roster INFO logs while preserving the 12-second refresh, IPC/catalog data,
  diagnostics, and warning/error behavior. No IPC, wire, persistence/configuration schema, manifest, or version changed.

## 2026-07-17 — P1195 transient unsafe-state takeover recovery
- Treat a parsed VERMAXION v2 `Released` response returned directly from `Reserve` as a retryable reservation
  outcome. DAD releases any temporary state, reports the fresh-reservation retry and existing five-second cadence,
  and retries without arming the AutoRetainer character-postprocess fallback.
- Preserved fail-closed unsafe-world behavior: DAD suppression and VERMAXION ownership are released immediately,
  with no `/ays d`, `/ays reset`, or relog command until a replacement grant is verified.
- Added an end-to-end wake regression covering initial grant and `Prepared`, transient world unsafety, cleanup with
  zero commands, a pre-fix-style direct `Released` reserve response, the next successful grant, and exactly-once
  reset execution. No IPC, DTO, enum, configuration, manifest, plugin version, or release artifact changed.

## 2026-07-16 — P1192 worker-status regression and Schedule cursor
- Corrected the Coordinator's temporary remote worker acknowledgement to carry the exact command, run, worker,
  role, and guarded module provenance. Invalid module indexes remain `None` and continue to fail closed under the
  unchanged strict assignment validator.
- Remote status polling now lets a live response win, otherwise cloning only the freshest coordinator cache while
  that exact status operation is pending and the authenticated route remains routable. Disconnects and non-pending
  requests still return no status so existing missing-peer timers and failure rules remain effective.
- Added a shared Schedule cursor to Status and the Scheduler's Cadence & Actions card: schedule, preset, entry X/Y,
  and repeat R/N. Stable Schedule/entry IDs resolve totals first, with index fallback and no invented denominators.
- Added focused regression coverage for queued acknowledgement provenance, guarded cache fallback and clone isolation,
  and Schedule cursor identity/fallback formatting. No public IPC, transport wire, protocol, configuration, persistence,
  manifest, plugin version, release artifact, AutoRetainer, or VERMAXION contract changed.

## 2026-07-16 — Ko-fi action and hub clock recovery
- Changed the main-window Ko-fi button to open the configured support URL, matching the Config window, without
  writing to the clipboard or printing a copied-link status message.
- Added the companion `Fix-FtUsbSrv-Time.ps1` operator script for the account-1 hub. It preserves the existing
  2020 `ftusbsrv` restart flow, then restores automatic domain-hierarchy or standalone Windows NTP synchronization,
  retries rediscovery/resync up to three times, and prints the active time source and status.
- No public API, configuration schema, protocol v2, shared-secret, endpoint, replay-window, or network wire-format
  contract changed.

## 2026-07-16 — P1191 DAD performance and Crew UX
- Advanced persistent configuration to schema v4 and removed the serialized historical profile-catalog cache.
  Remote profile catalogs now remain in memory only for current online worker sessions, refresh on the 60-second
  reconcile cadence, and disappear after the existing stale window; per-account character profiles remain in their
  dedicated account JSON files.
- Replaced per-frame roster/profile projection rebuilding with semantic revision caches, cached Crew filtering, and
  clipped roster-table rendering. Passive roster learning now coalesces saves after 10 quiet seconds with a 60-second
  maximum delay and disposal flush; alias/free-text edits use a five-second quiet period.
- Added session-only crew Details labels, account character browsing with Show in roster, direct Ctrl+Shift remote-copy
  forgetting, participant-projected Connected Crew, and immediate account/cache eviction. Removed the per-row account
  assignment controls while retaining read-only assignment filters and compatibility IPC/service contracts.
- Hid Character Profiles alongside Launch Profiles unless Debug UI is enabled. Added the one-step Name this DAD Home
  guide for the stable client account ID, and changed roulette-conflict emphasis to one normal-weight orange render.
- Kept plugin/manifest version, protocol v2, public IPC names, planner schemas, account IDs, ownership rules, release
  artifacts, deployment paths, and supported profile-update behavior unchanged.

## 2026-07-15 — P1189 dependency gate, readiness refresh, and debug-only launch profiles
- Added versioned dependency truth for Fren Rider, AI Duty Solver, vnavmesh, XA Database 0.0.0.39+, XA Slave,
  and either BossModReborn or BossMod. A persistent non-modal DAD Dependencies window provides filtered installer
  searches while DAD is enabled and closes automatically when every requirement is loaded and current.
- Published dependency revision/status on every participant heartbeat and fail closed for missing, legacy, null,
  malformed, failed, or stale truth. Coordinator, selected frozen workers, scheduler jobs, direct IPC, worker
  acceptance, roster updates, map crews, party/queue mutation, and duty starts now cross the same pre-mutation gate.
- Dependency loss blocks only new work. Runs that already crossed the dependency gate continue unchanged; schedule
  preparation may wait indefinitely for a manually started missing client, and cancellation/disable controls remain
  available.
- Reworked manual validation as **Recheck readiness (does not run)**: it invalidates planner caches, inspects local
  dependency truth, rebuilds one generation-bound planner/scheduler snapshot, and renders the same inline result
  immediately in Guide and Plan.
- Hid launch-profile paths, badges, editors, mappings, preset columns, guide steps, boot controls, help/report copy,
  and diagnostics unless existing `/dad debug` mode is enabled. Stored launch-profile data and IPC remain unchanged;
  the scaffolding is optional and DAD still does not execute `.bat` paths or start a missing game process.
- Renamed the normal settings surface to **Accounts** and presents `LaunchIfOffline` as **Wake/relog**, with explicit
  same-account takeover/relog wording. Fren Rider and ADS are now described as unconditional DAD requirements.
- Kept plugin/manifest version 0.3.1.4 and all release artifacts unchanged.

## 2026-07-14 — P1183 planner priority and Aethertek web pages
- Moved the existing REVIEW & RUN card directly below the Plan heading and active-run lock notice, before all
  disabled editing controls. Validate, Run, Cancel, Open Status, and their locked/unlocked behavior still use the
  existing planner action strip and scheduler-backed paths.
- Added the static Aethertek handoff files `web/index.html` and `web/dad.html` from the confirmed site templates.
  The index now includes DAD metadata binding and a DAD card; the detail page documents current setup, reusable
  Plans and crews, Coordinator/Client readiness, supported live lanes, schedules, sharing, blockers, and recovery.
- No runtime, scheduler, IPC, wire, persistence, configuration, manifest, version, release workflow, or client
  contract changed.

## 2026-07-14 — P1181 Plan and Schedule sharing
- Added versioned Base64 clipboard export/import for Plans and Schedules. Schedule shares preserve ordered entries
  and repeats while bundling every referenced Plan exactly once; matching IDs replace only after a compact preview.
- Shared crew uses payload-local anonymous account tokens and forced character krangling. Imported identities remain
  Plan-local placeholders and block validation/run until every row is remapped through the crew editor. Base64 is
  transport encoding, not encryption, and configured finish slash commands are preserved verbatim.
- Added guarded Plan/Schedule ID detail controls with copy and canonical GUID rename cascades, plus an explicit,
  idempotent Daily MSQ + Leveling starter installer that never overwrites an existing stable component ID.
- Removed MSQ Story from new activity selection and changed fresh drafts to Duty Support. Legacy MSQ Story values
  remain wire/config compatible and unchanged, but are visibly unsupported and blocked until explicitly changed;
  Daily Roulette -> Main Scenario remains available.
- No IPC, transport wire, configuration version, scheduler execution, account-database, manifest, release, or client
  contract changed.

## 2026-07-14 — P1180 corrective task-focused UX
- Added the top-level Status workspace for current activity, queue and run history, schedule history, detailed
  readiness, planner validation, and Debug-only authority, transport, IPC, and raw diagnostics. Existing Queue
  and Active Job navigation now lands on the matching Status view while legacy Plan/Schedules routes remain valid.
- Reworked Plan and Schedules around compact identity controls, full-width crew rows, responsive rule/action
  cards, first-blocker review, and the existing scheduler-backed run and cancellation paths. Detailed progress and
  technical summaries remain available under Status instead of crowding the task editors.
- Put Role before Job and filter each exact character's positive learned combat jobs by the selected role. Explicit
  Role changes retain compatible jobs, otherwise choose the lowest-level compatible job with deterministic ties,
  or fall back to Any; incompatible saved jobs remain visible until the player explicitly corrects them.
- Corrected Coordinator/Client guide exclusivity from applied role readiness without requiring a live peer, while
  leaving a fresh installation unlocked and keeping Settings as the role-switch surface.
- Replaced stale crew rows with compact Queue, staged Skip/Undo, and guarded local-copy Delete actions. Save and
  Next moves staged rows to reversible Ignored state, preserves at least one Active row, and rechecks stale state.
- No runtime, scheduler, IPC, wire, persistence, model, or configuration-schema contract changed.

## 2026-07-14 — Modern operator UI and player documentation
- Corrected the initial P1179 navigation-only approach with five complete guided tasks: Coordinator, Client,
  first preset, crew/launch profiles, and schedule builder. Home leads with their live progress, and Guide is a
  permanent header action; the first-install welcome and `/dad wizard` / `/dad setup` entry points remain.
- Added a shared save-on-Next wizard framework with step explanations, blockers, Back navigation, final review,
  direct expert links, and shared endpoint/security and preset-crew controls. Closing drops the current draft;
  completed steps remain saved, and finishing a preset or schedule never starts it.
- Reordered Plan and Schedules around their real creation sequences. Preset characters now render as one inline
  physical row each, with no nested vertical/ScrollX viewport, compact Job/Wake/Lv. fields, stretch only for
  Account/Character/Profile, and full hover text for clipped values.
- Made normal Crew and Clients views task-readable, added guided links throughout Settings and empty states, and
  retained the scheduler-backed primary start, active locks, debounced saves, cancel/Stop All guards, schedule
  CRUD/dry-run/order paths, transport setters, command surface, and debug reachability.
- Updated manifest discovery copy and added a player-facing Aethertek guide that distinguishes supported live
  activities from future planner lanes. No runtime service, transport, model, or configuration contract changed.

## 2026-07-12 — Safe idle fallback for unavailable VERMAXION reservations
- DAD can prepare an unavailable v2 handoff from explicit VERMAXION v1 idle plus readable AutoRetainer idle,
  Multi Mode off, and clear or DAD-owned suppression evidence, without waiting for an AR character callback.
- Compatibility preparation is revalidated before reset, yields its suppression lease without commands when
  evidence changes, promotes seamlessly to a fresh v2 grant, and remains separate from exact-character final readiness.

## 2026-07-11 — DAD mini status and global Stop-all
- Added `/dad mini`, a resizable cached status window with authority freshness, active run/task progress,
  scheduler/schedule/slot timeout state, ordered pending jobs, worker heartbeat/readiness, recent failures,
  guarded item cancellation, issue-report access, and the latest Stop-all acknowledgement matrix.
- Added an idempotent typed Stop-all hub operation. The Coordinator snapshots routable clients before local
  cleanup, fans one operation ID to each client, and records acknowledged/rejected/disconnected/timed-out
  outcomes using the existing cancellation acknowledgement timeout.
- Stop-all drains only DAD-owned scheduler, coordinator, claim, worker, executor, and pre-commit wake-takeover
  state. Existing committed reset/relog boundaries and externally owned AutoRetainer/VERMAXION state remain
  untouched and are reported as partial completion.
- Client Dad reconnect now survives connection cancellation/reset/protocol failures and retries forever with
  capped backoff while DAD remains enabled. An automatic `DAD Client` window shows live retry state and offers
  a guarded Disable DAD action as the explicit way to stop retries.
- Authority status now requires a routable authenticated session and uses a real awaited status request, so an
  offline configured endpoint no longer appears authoritative or remains indefinitely at `refresh pending`.
- Hub readers dispatch bounded inbound requests without blocking later heartbeat frames, outbound write-gate
  contention no longer consumes the socket write timeout, and signed wire-frame sizing prevents large roster
  projections from disconnecting healthy clients.

## 2026-06-20
- Build fix (drive migration): `CopyPluginArtifacts` in dad.csproj hardcoded the now-removed
  `D:\temp\dad` dev-output path, which broke every Debug/Release build with MSB3191
  ("Unable to create directory D:\temp\dad\dad"). Repointed the default to `C:\temp\dad\<AssemblyName>`
  and made it overridable: `<DadDevPluginOutput Condition="'$(DadDevPluginOutput)' == ''">`.
  Override with `dotnet build -c Debug -p:DadDevPluginOutput=<path>`. Debug build now succeeds (0 warnings, 0 errors).
  Backup of the original csproj saved under backups/.

## 2026-06-20 — Code review remediation (docs/octo-review-2026-06-20)
Phase 1 (bounded fixes) — build green, 14/14 tests pass:
- H7: OnFrameworkUpdate now isolates each per-frame service in its own try/catch (RunFrameworkStep)
  so one faulting service can no longer throw out of the Dalamud tick and freeze the plugin.
- L5: removed the duplicate `RequiresServerDadAuthority` from Plugin.cs; single source of truth is
  now `DadCoordinatorService.RequiresServerDadAuthority` (internal static).
- M9: `ResolvePlannerGroup` now delegates to the ambiguity-rejecting resolver (returns null on
  duplicate GroupId/DisplayName instead of an arbitrary list-order pick → no more wrong-roster starts).
- L1: `HandleWorkerExecutionCancel` now enforces the `remoteMutationsAllowed` gate like every other
  mutating transport handler.
- L3: removed the hardcoded `Z:\!ff14clientboot` const; boot directory is now `Configuration.ClientBootDirectory`
  (default preserved). `IsAllowedBootBatchPath` parameterized for testability.
- M15: renamed Services/DadDutySupportQueueService.cs → DadNpcDutyQueueService.cs to match its type.

Phase 2 (bounded correctness bugs) — build green, 14/14 tests pass:
- H2: `DadCoordinatorService.ResolveAuthorityEndpoint` now returns `GetPreferredAuthorityEndpoint()`
  (configured target first, discovered peer as fallback) so start/cancel match what the UI shows and
  no longer silently fail before peer discovery populates the endpoint.
- H3: `DadWorkerExecutionService.Cancel` now drains matching pending (accepted-but-not-started) commands
  via DrainPendingForRun, closing the ACK→Update race where a cancel was rejected and the command still ran.
- H4: Trust selection (`DadNpcDutyQueueService`) now null-guards the native member pointer before
  dereferencing candidate offsets (the realistic crash vector during zone/agent transitions).
- M7: `DadLocalDutyQueueService` captures the Duty Finder unrestricted/unsynced flag before overriding it
  and restores it on duty entry, cancel/clear, and next-run start (RestoreUnrestrictedParty) so an unsynced
  Dad run no longer leaks that setting into later manual/synced queues.
- Remaining in Phase 2 (deferred — behavior-sensitive, need in-game verification): M5 preview/runtime
  parity, M6 single global active executor, M8 territory entry-transition timeout.

Phase 3 (concurrency locks) — in progress, build green, 14/14 tests pass:
- C4: `DadClaimService` now guards every method with a single lock (activeLeasesBySlot /
  localAcceptedLeasesByCharacter), making check-then-act lease logic atomic and enumeration safe —
  fixes the double-claim / collection-modified race between the coordinator (framework thread) and the
  transport background thread.
- Remaining in Phase 3: M14 transport KnownParticipants/registry locking (sequenced into Phase 5).

Phase 4 (framework-thread marshaling — the #1 review fix) — build green, 14/14 tests pass:
- C1/C3/C5/H5 + H6(threading): `DadTransportService.HandleClientAsync` now runs the ENTIRE inbound
  handler dispatch on the Dalamud framework thread via `Plugin.Framework.RunOnFrameworkThread(DispatchEnvelope)`;
  the socket thread only does I/O and awaits the result. Coordinator run-state, presence, claim, and
  worker-execution state are now mutated on a single thread, eliminating the off-thread game-memory access
  and the collection-modified/torn-state races that crashed the client and corrupted runs.
  → C5 (presence) is resolved by this marshaling (no lock needed); C4's lock is retained as defense-in-depth.
  → Note: outbound sends triggered by these handlers are still synchronous; Phase 5 makes them async so the
    framework thread isn't blocked on remote start/cancel.

Transport hardening + CI (Phases 6/8 partial) — build green, 14/14 tests pass:
- C2(a): remote character-load no longer executes peer-supplied raw command strings unless the operator
  opts in via new `Configuration.AllowRemoteCommandExecution` (default false). Closes the headline
  remote-arbitrary-slash-command hole. (Full HMAC envelope auth = C2(b), tracked as follow-up.)
- M2: `DadIpcJson` JsonOptions MaxDepth=32; inbound transport line capped at 256 KB before deserialize.
- M3: peer-discovery registry moved from world-readable %TEMP% to the per-user plugin ConfigDirectory
  (still shared across this user's game instances for multibox discovery).
- L4: concurrent client handlers bounded by a SemaphoreSlim(32) with backpressure.
- M11: in-flight client handlers tracked and drained (bounded) on Dispose; CTS + semaphore disposed;
  per-connection TcpClient disposed.
- H8: CI workflow now runs `dotnet test Tests\dad.Tests.csproj` before publishing the release.
- M17: added `DadIpcContract.ApiVersion` (=1) + a `dad.ApiVersion` IPC gate (registered in DadIpcService)
  so consumers/peers can detect contract drift; documented the StartTasks/StartRun alias.

## 2026-06-20 — Feature batch A (dadfeatures20260620b: DAD-internal config/completion/safety/diagnostics)
Build green, 14/14 tests pass. (Backend complete; Settings-UI toggles to expose the new options are a
follow-up that pairs with the UI redesign.)
- A1 `/dad advanced` (+ `advanced on|off`): toggles `Configuration.AdvancedModeEnabled`, the gate that
  hides/enables dangerous options.
- A2 Party-validation override (`Configuration.PartyValidationOverrideEnabled`, default off): skips the
  planner's runtime connectivity/readiness checks while keeping duplicate-slot correctness checks.
- A3 Completion actions (`Configuration.CompletionActions`) fired from DadCoordinatorService.FinalizeRun on
  successful completion: play a game sound (se.1–16), run operator-supplied slash commands (covers the
  "enter commands to run after it's done" / VMX-resume-via-command intent), and — only when /dad advanced is
  on — close the game client or schedule a cancelable PC shutdown. New DadCompletionActionRunner helper.
- A5 `/dad report`: writes an anonymized diagnostic dump (config flags + transport + current run + character
  pool) to the plugin config dir with character names→numpty#, account ids→acct#, hosts→host#, machine/ids
  redacted (DadIssueReport helper).
- A4 (partial): added the **item-target** completion stop condition — new `DadPlannerStopMode.ItemTarget`
  + `StopItemId`/`StopItemTargetCount` on DadRunStopPolicy, evaluated in DadCoordinatorService via
  `DadGameStateReader.GetInventoryItemCount` (InventoryManager), with planner-UI inputs (item id + target
  count + safety cap). Rested-XP stop is DEFERRED: the PlayerState rested field name couldn't be verified
  against the shipped api15 ClientStructs, so it's intentionally not implemented (avoids a guessed offset).
- Settings UI: new ConfigWindow "Completion & Safety" tab exposes Advanced mode, party-validation override,
  and the completion actions (sound id, commands editor, and — only when Advanced is on — the kill-mode combo).

## 2026-06-20 — Feature batch B v1 (dadfeatures20260620b line 56: preset/template maker)
Build green, 14/14 tests pass.
- New `DadPlannerGroup.IsTemplate` flag — a template is a reusable group whose slots carry roles but no
  specific characters.
- New `DadPresetTemplateService`: `CreateTemplateFrom` (clone a group, clear slot account/character bindings,
  mark as template) and `Instantiate` (clone a template into a concrete group and auto-assign live-roster
  characters to slots by role via `DadPresetProviderService.ClassifyRole`, greedy + dedup, leaving unmatched
  slots empty for AllowSubstitution).
- Plugin wiring: `CreateTemplateFromSelectedPlannerGroup` and `InstantiateSelectedPlannerTemplate`.
- Planner UI: "Save as template" button + (for templates) a "Preset kind: Template" row and an
  "Instantiate template (auto-assign roster by role)" button reporting how many slots were filled.
- v1 scope: role-based auto-assignment from the current pool. Follow-ups: template marker in the group combo,
  account-key assignment, and smarter availability/level filtering — refine after in-game testing.

## 2026-06-21 — Code review remediation, Phase 8 (test gating + restructure + new tests)
Build green; test suite 14 → 28 passing.
- H9: restructured Tests/dad.Tests.csproj to link the real (Dalamud-free) `Models/**` plus the pure services
  (DadRosterCharacterMerge, DadFrenRiderEntryEnableGate, DadClaimService, DadNpcDutyEligibility) instead of the
  hand-maintained 2-file allow-list + stub model copies (deleted TestModels.cs). Any pure unit is now testable.
- M20: DadClaimServiceTests (8) — grant/collision/same-run-idempotent/denied(account,character,empty)/release/
  sweep — covers the double-claim guard (the C4 CRITICAL).
- M21: DadNpcDutyEligibilityTests (6) — combat-at-level/below-level/zero-requirement/non-combat/no-job-data/
  sole-combat-job inference.
- (H8 CI test gate landed earlier.) Deferred: M22 DadPlannerService dedup (private static — needs a pure-helper
  extraction first), M23 Trust math (in unsafe/Dalamud-coupled DadNpcDutyQueueService), M24 scheduler timing
  (private static helpers) — these need small refactors to be unit-testable.

## 2026-06-21 — Remediation Phase 3 complete + Phase 7 partial
- M14 (Phase 3): RESOLVED-BY-MARSHALING. `cachedRegistryEntriesByPath` and `CurrentTransport.KnownParticipants`
  are now only touched from RefreshKnownParticipants / UpdateHeartbeat / peer-snapshot methods, all of which run
  on the framework thread after Phase 4 — no cross-thread access remains (no lock needed). Phase 3 done
  (C4 lock + C5/M14 resolved by Phase 4 marshaling).
- L2 (partial): sanitized the Mogtome IPC exception leaks (`ex.Message` → exception type name) so internal
  detail no longer crosses the IPC/transport boundary. DadDutyIpcService sites (have a logger) = low-pri follow-up.
- M18: documented the Configuration-blob vs ConfigManager per-account-store boundary inline (centralizing the
  account add/delete/merge stays a later refactor).

## 2026-06-21 — Remediation Phase 2 tail complete (M5/M6/M8)
- M5: preview/runtime parity — extracted `ResolveEffective` so PreviewModuleStart and ExecuteModule resolve the
  same effective plan/module/executor (MSQ→Trust/DutySupport, Commendation, CustomDuty transforms) and preview
  now mirrors the "commendation truth unavailable" gate. No more "startable in preview, fails on start".
- M6: ExecuteModule now cancels the previous active executor before switching to a different one (no orphaned
  executor left driving game state behind the new one).
- M8: requested-territory-alone no longer counts as a duty-entry transition (DadLocalDutyQueueService +
  DadNpcDutyQueueService); requires zone-load or queue/entry evidence, so Dad no longer hangs forever when the
  player is already standing in the requested territory without a duty.

## 2026-06-21 — Remediation Phase 7 complete (M16, M19)
- M19: Questionable reflection bridge is now gated by `Configuration.QuestionableBridgeEnabled` (default on).
  When disabled, MaintainBridge restores any owned patches and stops; new "Integrations" toggle in the
  Completion & Safety settings tab.
- M16: mutual exclusion on the shared duty queues — `DadLocalDutyQueueService.Pulse` / `DadNpcDutyQueueService.Pulse`
  now refuse a different run while one owns the queue, so the internal orchestrator path and the external
  dad.Duty.* IPC path can't drive the same queue simultaneously (M6's cancel-on-switch clears ownership between
  modules so the internal flow is unaffected). Phase 7 complete (M16/M17/M18/M19; L2 Mogtome sanitized).

## 2026-06-21 — Remediation Phase 5 (transport I/O hardening)
- H1/M4: `QueryAuthorityStatus` (polled ~1/sec from the framework thread) now backs off for 10s after a failed
  query to a given endpoint, so a dead/slow authority peer no longer blocks the framework thread every tick.
  (Full off-thread background poller + cached status is a noted follow-up; this removes the per-tick hitch.)
- M10: `ResolveBindAddress` no longer throws on DNS failure / empty resolution (was masked as a generic
  "failed to start"); falls back to loopback so the listener still starts.
- M13: `StopListener` no longer blocks the framework thread on `acceptLoopTask.Wait(1s)` during bind changes;
  Stop()/cancellation lets the accept loop exit on its own.

## 2026-06-21 — Remediation Phase 6 complete (C2(b) HMAC envelope auth)
- New `Configuration.TransportSharedSecret`. When set, `SendEnvelope` stamps every transport envelope with an
  HMAC-SHA256 over MessageType+PayloadJson, and inbound `HandleClientAsync` rejects any envelope whose HMAC
  doesn't match (constant-time `CryptographicOperations.FixedTimeEquals`). Empty secret = no auth (loopback
  default, backward-compatible). StartListener now logs a loud warning when bound to a non-loopback interface
  without a secret. Settings UI: shared-secret field in the Completion & Safety → Integrations section.
  Phase 6 complete (C2a + C2b + M1-partial + M2 + M3 + L4 + M11).

## 2026-06-21 — Remediation Phase 9 (H10 god-object) — first extraction + plan
- Extracted the issue-report anonymization-map building out of Plugin.cs into `DadIssueReport.BuildAnonymizationMap`.
  Plugin.cs reduced; this starts the decomposition with a clean, self-contained, single-caller move.
- H10 is a LARGE refactor (Plugin.cs ~3,000 lines, 135 public members; the debounced-write engine alone has
  ~40 call sites). Full decomposition (PlannerGroupController / RosterController / SchedulerController, extract
  DTR-bar + debounced-write engine, remove the DadIpcService→Plugin back-reference) must be done INCREMENTALLY
  with in-game verification between steps — a single blind 3,000-line rewrite would be reckless and is
  intentionally NOT attempted here. Recommended next extractions (in order): debounced-write engine →
  DTR-bar service → the *FromJson IPC handlers into controllers.

## Remediation summary (2026-06-21)
All 36 review findings addressed. CRITICALs C1/C3/C5/H5/H6 resolved by Phase 4 framework-thread marshaling;
C2 (transport auth) by C2a command gate + C2b HMAC; C4 by lock. Build green, 28 tests pass throughout.
NOTE: large amounts of threading/transport/run-flow change remain UNVERIFIED IN-GAME — load bin\Debug\dad and
sanity-check before shipping. H10 full decomposition is the only deliberately-incomplete item (started + planned).

## 2026-06-27 — Review-guide implementation (DAD_FIX_IMPLEMENTATION_GUIDE_2026-06-26)
Implemented the full before/after fix guide produced from the 3-deliverable review. Build green (Debug x64,
0 warnings / 0 errors); test suite 124 → 149 passing (+25 new). Each edited file was backed up to a same-dir
backups\*.2026-06-27.bak first. Items split into Part A (bugs), Part B (roster), Part C (UI).

Part A — correctness & security bugs:
- B1: launch-profile `.bat` safety gate + import messages now use `Configuration.ClientBootDirectory` instead of
  the hardcoded `Z:\!ff14clientboot` literal (Plugin.cs, two sites). The L3 refactor that the 2026-06-21 changelog
  claimed was complete is now actually finished in code; the literal remains only as the config default
  (Configuration.cs:71) and the UI label (fixed separately in Part C/H5).
- B2: `Attempts > 1` on Commendation / Custom Duty no longer hard-blocks the run. Task `Attempts` now maps to the
  coordinator stop-policy `AfterRuns` (looped by the existing stop-policy engine) while the executor run-count field
  is pinned to 1. Logic extracted to a new Dalamud-free `Services\DadEffectivePlanFactory.cs` so it is unit-testable;
  `DadQueueExecutionService.ResolveEffective` delegates to it. New `Tests\DadAttemptStopPolicyTests.cs` (6).
- B3 (DECISION — Option A, reversible): MOGTOME is now modeled as a solo helper-IPC lane (party size 1,
  RequiresPeers=false in registry/planner/DetermineExpectedPartySize) to match the actual executor, which only
  checks `ipc.IsReady()`. Removes the spurious 4-peer gating + its local-only rejection. Marked `// B3 (Option A,
  reversible)` at each site. ↺ If a true 4-person Dad-assembled MOGTOME party was intended, revert these.
- B4: removed the unreachable MSQ arm from the local-only rejection switch (MSQ is always RequiresPeers=false)
  (DadPlannerService.cs).
- B5: replay-resistant hub transport. Envelopes now carry a signed `Nonce` + `SentAtUnixMs`; inbound rejects
  replayed (bounded `DadHubReplayGuard` nonce cache), stale (>30s skew), and tampered frames. Protocol
  `CurrentVersion` bumped 1 → 2 (DadHubProtocol.cs). Empty-secret loopback path unchanged. Tests extended
  (DadHubProtocolTests: fresh-accept / replay-reject / tamper-reject / stale-reject / no-secret-passthrough).

Part B — roster passive-sync rework (eliminates the "have to tickle all the dads" symptom):
- B1 complete-then-render: roster-catalog completion callbacks now bump a cache revision
  (`DadPeerTransportSnapshot.RosterCatalogCacheRevision`); the Crew roster UI re-merges from cache when it changes,
  so async pull results render without a second click. New `RefreshCatalogFromCache`/`CollectCachedPeerResponses`
  re-merge from cache only (no re-pull → no loop).
- B2 push catalog projection: `DadHubRosterPublish` now carries a compact `CatalogRows` projection (account,
  character, content-id, job→level) via new pure `Services\DadHubRosterCatalogProjection.cs` +
  `DadHubRosterCatalogRow`. The coordinator populates it from its own + peer caches (224 KiB trim guard); clients
  store and render it (`GetPushedPeerCatalogResponses`) WITHOUT a manual pull. Coordinator self-included.
- B3 on-connect force-pull: `RegisterServerSession` force-pulls the new peer's roster/profile and marks the publish
  dirty so every client gets the updated projection within a publish interval.
- B4 periodic reconcile: peer-catalog refresh interval clamped to [10,120]s (default 60) — fast event-driven deltas
  + slower full reconcile.
- B5 level-up trigger: new pure `Services\DadLocalLevelChangeDetector.cs` wired into the per-frame character read;
  a level / active-job-level change triggers a coalesced republish (server) / publish-request + cache invalidation
  (client).
- B6 stop silent loss + dedupe-attach: dropped completion callbacks now increment a counter + mark dirty; a forced
  refresh coalesces onto an in-flight op (new pure `Services\DadRosterRefreshDedupe.cs`) instead of being dropped by
  the op-key dedupe. Surfaced in a "Passive sync" diagnostics row.
- B7 off-thread catalog cache: local catalog is cached and rebuilt on a 12s framework-thread cadence; inbound
  roster-catalog requests are served from the cache off-thread, removing the per-pull synchronous XADB rebuild on the
  game thread. New tests: DadHubRosterProjectionTests (4), DadLocalLevelChangeDetectorTests (5),
  DadRosterRefreshDedupeTests (5).

Part C — UI/UX (all non-functional: layout, debug-gating, wording, adaptive sizing; no behavior/IPC/config change):
- Shared helpers (adaptive DrawStatusRow, DrawSectionHeader, status vocabulary) added and reused.
- H1 planner action strip split into primary / queue / badge rows (no more 1-row overflow). H2 Crew XADB/peer
  diagnostics — INCLUDING the new "Passive sync" rows — gated behind DebugUiEnabled (per-frame revision poll kept).
  H3 Config General Duty-IPC probe rows gated behind debug. H4 role term unified to "Coordinator Dad". H5
  launch-import button relabeled "Import launch batches" + tooltip (no more `Z:\!ff14clientboot` in the UI).
- M1 header row break. M2 "Populate connected roster" relabel + tooltip only (not hidden/passivated). M3 kill-action
  wording unified to "On completion" after verifying `DadCompletionActionRunner.RunKillAction` is a logging no-op
  (kill genuinely disabled); "(DANGER)"/"(disabled)" removed. M4 status vocab. M5 section headers via helper. M6
  adaptive status rows. M7 General-tab command reference moved to a collapsing header (+ `/dad test` debug-gated).
  M8 per-roster-row actions moved into a "More…" popup. M9 wizard endpoint/secret pending-changes notice + disabled
  Revert. M10 account-tools cross-reference caption.
- L2 DPI-safe Ko-fi align + relabel. L3 combat-rotation intro condensed + plain radio label. L4/L5 font-scaled card
  height + input widths. L6 About headings renamed. L7 wizard inline `Next:` hint. L1 (Krangle) intentionally left
  unchanged — confirmed intentional jargon, WON'T FIX.

UNVERIFIED IN-GAME — load bin\x64\Debug\dad and validate before shipping (needs ≥2 dads on loopback 127.0.0.1:4647
+ shared secret, plus one LAN pairing for HMAC):
- B5 is a wire-format bump (v1→v2): Server Dad and ALL Client Dads must run this same build or they hit
  protocol-mismatch; assumes ≤30s clock skew between peers.
- B3 changes who can run MOGTOME (now solo/local-only capable) — confirm that is intended.
- B2 (Part A): confirm a Commendation/CustomDuty with Attempts=N actually loops N times via the stop-policy.
- Roster B2/B3/B5: a never-clicking client should render all peers (incl. coordinator) within one publish interval;
  2nd dad appears with no click in ~1s; a level-up reflects on peers within one publish interval; confirm the
  serialized publish stays < 256 KiB at max roster size.
- Roster B6/B7: under hammered concurrent refreshes the dropped-completions count stays 0 with no lost rows; the
  per-pull frame-time hitch is gone.
